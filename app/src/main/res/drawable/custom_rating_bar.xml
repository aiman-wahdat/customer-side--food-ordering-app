<!-- custom_rating_bar.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background"
        android:drawable="@drawable/ic_star_gray" /> <!-- Gray star drawable for background -->
    <item android:id="@android:id/secondaryProgress"
        android:drawable="@drawable/ic_star_gray" /> <!-- Gray star drawable for progress -->
    <item android:id="@android:id/progress"
        android:drawable="@drawable/ic_star_yellow" /> <!-- Yellow star drawable for selected stars -->
</layer-list>
